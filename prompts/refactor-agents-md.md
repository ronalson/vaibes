You are refactoring or generating an AGENTS.md file.

This file is static context read at the start of every session.

Your job:
- Extract only what is essential to work correctly in this repo
- Remove anything obvious, vague, or redundant
- Consolidate to a single source of truth (link instead of copy)

The file must contain:
- One-sentence project summary
- Copy-pasteable commands (wrapped in backticks)
- Rules that prevent breaking builds, tests, or architecture
- References to canonical examples in the codebase

Rules:
- ≤150 lines
- Short bullet points
- No nested explanations
- No task-specific instructions

If instructions conflict, FLAG the conflict instead of guessing.
If unsure whether something belongs here, exclude it.
